// 1. Define he macros (do this in ONLY ONE file in your project)
#define STB_IMAGE_IMPLEMENTATION
#define STB_IMAGE_WRITE_IMPLEMENTATION

// 2. Include the headers
#include "../include/stb_image.h"
#include "../include/stb_image_write.h"

#include <iostream>

int main() {
    std::cout << "CUDA Augmentation Project Initialized." << std::endl;
    std::cout << "stb_image loaded successfully." << std::endl;
    
    // We will put the image loading logic here next.
    
    return 0;
}
